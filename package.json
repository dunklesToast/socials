{
  "name": "socials",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon app.js & sass --no-source-map --watch styles/:public/css --style compressed",
    "start": "node app.js",
    "build": "sass styles/:public/css/ --style compressed --no-source-map"
  },
  "dependencies": {
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "express-minify-html-2": "^1.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "prettier": "^2.3.0",
    "sass": "^1.34.0"
  }
}
