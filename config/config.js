const config = {
  defaultuser: 'example',
  allowEdit: true,
  // needs allow edit on true to work
  allowCreate: true,
  // "/" refers to app working directory (root of project)
  peopleDirectory: '/config/people',
};

module.exports = config;
